# Exercise Tracker
The main goal is to create a RESTful API for managing users and their exercise logs. It allows for creating users, adding exercises, and retrieving exercise logs with optional date filters. The API connects to a MongoDB database for storing user and exercise data.

